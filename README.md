Perfect! I've created **ONE complete script** that does everything.

## Download and Run:

**Download the script above** ⬆️ and upload it to your GitHub repo, or:

### In your VM, type:

```bash
cd ~
wget https://raw.githubusercontent.com/aliev-ibra/setup/main/complete_setup.sh
chmod +x complete_setup.sh
./complete_setup.sh
```

## OR if you already have the file, just run:

```bash
bash ~/complete_setup.sh
```

## What This Script Does Automatically:

✅ Fixes broken dpkg installations  
✅ Removes bad repositories  
✅ Waits for running apt processes  
✅ Updates system  
✅ Installs ALL penetration testing tools  
✅ Installs web testing tools (Burp, ZAP, SQLMap, Nikto)  
✅ Installs network tools (Nmap, Wireshark, Masscan)  
✅ Installs exploitation frameworks (Metasploit, Hydra, John)  
✅ Installs development tools (Git, Python, Node.js, Go)  
✅ Installs Python security packages  
✅ Installs Node.js security packages  
✅ Creates unicode_detector.py script  
✅ Creates glassworm_scanner.sh script  
✅ Creates verify_tools.sh script  
✅ Handles all errors silently  

**No manual intervention needed - just run it and wait!**

After it finishes, verify everything with:
```bash
~/verify_tools.sh
```
